import React from 'react';
const Loading: React.FC = () => {
  return (
    <svg className='h-3 w-3 animate-spin border-[3px] border-white rounded-full border-t-transparent'></svg>
  );
};

export default Loading;
